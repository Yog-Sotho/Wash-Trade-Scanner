# Developer & testing dependencies. Kept in sync with the `[project.optional-dependencies.dev]`
# table in pyproject.toml, which is the source of truth (`pip install -e ".[dev]"`).
# Linting/formatting/security tooling standardized on ruff + mypy + bandit + pip-audit
# (see CI: .github/workflows/CI.yml).
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
pytest-cov>=4.1.0

ruff>=0.2.0
mypy>=1.8.0
types-python-dateutil>=2.8.0
pandas-stubs>=2.2.0
types-networkx>=3.2.0

coverage>=7.4.0
hypothesis>=6.98.0

pip-audit>=2.7.0
safety>=3.0.0
bandit>=1.7.7
cyclonedx-bom>=4.0.0
